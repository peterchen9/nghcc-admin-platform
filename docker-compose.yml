name: nghcc-admin-platform

services:
  web:
    container_name: nghcc-admin-web
    build:
      context: ./backend
      dockerfile: Dockerfile
    env_file:
      - .env
    environment:
      DB_HOST: db
      DB_PORT: 3306
      DB_NAME: ${DB_NAME:-nghcc_admin}
      DB_USER: ${DB_USER:-nghcc_admin}
      DB_PASSWORD: ${DB_PASSWORD:-change_me_password}
      DB_PASS: ${DB_PASSWORD:-change_me_password}
      UPLOAD_DIR: /app/media
    ports:
      - "${APP_PORT:-26001}:8000"
    volumes:
      - ./uploads:/app/media
    depends_on:
      db:
        condition: service_healthy
    restart: unless-stopped
    networks:
      - nghcc-admin-network

  db:
    container_name: nghcc-admin-db
    image: mysql:8.0
    environment:
      MYSQL_DATABASE: ${DB_NAME:-nghcc_admin}
      MYSQL_USER: ${DB_USER:-nghcc_admin}
      MYSQL_PASSWORD: ${DB_PASSWORD:-change_me_password}
      MYSQL_ROOT_PASSWORD: ${DB_ROOT_PASSWORD:-change_me_root_password}
    ports:
      - "${DB_EXTERNAL_PORT:-26003}:3306"
    volumes:
      - nghcc-admin-mysql-data:/var/lib/mysql
    healthcheck:
      test: ["CMD-SHELL", "mysqladmin ping -h 127.0.0.1 -u${DB_USER:-nghcc_admin} -p${DB_PASSWORD:-change_me_password} --silent"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped
    networks:
      - nghcc-admin-network

volumes:
  nghcc-admin-mysql-data:
    name: nghcc-admin-mysql-data

networks:
  nghcc-admin-network:
    name: nghcc-admin-network
